// Hand-written content overrides for Level 1 terms.
// Keyed by slug. Provides oneLiner/beginner/technical/whyItMatters/vibeCodingUsage for both locales,
// plus useCases / commonMisunderstandings / promptExample / codeExample when available.
// Anything missing falls back to the templated content in generate-terms.mjs.

export const OVERRIDES = {
  // Note: ai and llm are already hand-written in committed data; overrides apply to the rest.
};
